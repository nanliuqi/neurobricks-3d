# Edge 浏览器比例异常 - 快速修复指南

## 🚨 立即尝试的解决方案

### 方法 1：重置浏览器缩放（最常见原因）⭐

**在 Edge 浏览器中按：**
```
Ctrl + 0 (数字零)
```

或者：
1. 点击右上角三个点 `...`
2. 找到"缩放"选项
3. 设置为 **100%**

---

### 方法 2：清除浏览器缓存

**快捷键：**
```
Ctrl + Shift + Delete
```

选择：
- ✅ 缓存的图片和文件
- ✅ Cookie 和其他网站数据

点击"立即清除"，然后刷新页面（F5）

---

### 方法 3：检查 Windows 显示设置

1. 右键桌面 → **显示设置**
2. 找到"缩放与布局"
3. 确保设置为以下之一：
   - ✅ **100%** (推荐)
   - ✅ **125%**
   - ✅ **150%**
   - ❌ 避免使用 110%、135% 等非标准值

4. **注销并重新登录**使设置生效

---

### 方法 4：使用无痕模式测试

**打开无痕窗口：**
```
Ctrl + Shift + N
```

访问 `http://localhost:5173`

如果正常，说明是扩展程序或缓存问题。

---

## 🔍 诊断步骤

### 1. 运行自动诊断脚本

在 Edge 浏览器中：

1. 按 **F12** 打开开发者工具
2. 切换到 **Console** (控制台) 标签
3. 你会看到自动运行的诊断输出

**或者手动运行：**
```javascript
diagnoseDisplay()
```

**关键检查项：**
- ✅ CSS 缩放比例应该是 **1.0**
- ✅ Canvas 像素比应该匹配设备像素比
- ✅ 设备像素比应该在 **1.0 - 2.0** 之间

---

### 2. 常见问题诊断

#### 问题 A：CSS 缩放比例不是 1.0

**症状：**
```
⚠️ CSS 缩放比例: 1.25 - 按 Ctrl+0 重置为 100%
```

**解决：**
```
按 Ctrl + 0
```

---

#### 问题 B：Canvas 像素比不匹配

**症状：**
```
❌ Canvas 像素比不匹配!
实际: 1.00
期望: 1.25
差异: 0.25
```

**解决：**
1. 按 Ctrl+0 重置缩放
2. 刷新页面（F5）
3. 如果仍有问题，检查显卡驱动

---

#### 问题 C：设备像素比异常

**症状：**
```
⚠️ 设备像素比异常: 2.5 - 建议范围: 1.0 - 2.0
```

**解决：**
调整 Windows 显示缩放到标准值（100%、125%、150%、200%）

---

## 🎯 验证修复

### 检查清单

完成修复后，确认以下几点：

- [ ] 浏览器缩放为 100%（地址栏右侧显示）
- [ ] 界面元素位置正确，无错位
- [ ] 3D 场景清晰，无模糊
- [ ] 拖拽功能正常工作
- [ ] 控制台无红色错误

### 对比测试

**在 VSCode 内置浏览器中：**
1. 打开命令面板：`Ctrl + Shift + P`
2. 输入：`Simple Browser: Show`
3. 访问 `http://localhost:5173`
4. 观察比例是否正常

**在 Edge 浏览器中：**
1. 访问 `http://localhost:5173`
2. 按 F12 查看诊断输出
3. 确认所有检查项通过

---

## 🛠️ 高级排查

### 如果以上方法都无效

#### 1. 检查 Edge 实验性功能

1. 地址栏输入：`edge://flags`
2. 搜索：`hardware acceleration`
3. 确保 **Hardware-accelerated GPU scheduling** 为 **Enabled**
4. 重启浏览器

#### 2. 更新显卡驱动

1. 设备管理器 → 显示适配器
2. 右键显卡 → 更新驱动程序
3. 或访问显卡官网下载最新驱动

#### 3. 禁用硬件加速（临时方案）

1. Edge 设置 → 系统和性能
2. 关闭"使用硬件加速（如可用）"
3. 重启浏览器

⚠️ **注意：** 这会降低 3D 渲染性能，仅用于测试

#### 4. 检查多显示器设置

如果有多个显示器：
1. 确保主显示器设置正确
2. 尝试将浏览器窗口移动到主显示器
3. 检查每个显示器的缩放设置是否一致

---

## 📊 常见场景对照表

| 场景 | 设备像素比 | 浏览器缩放 | Windows 缩放 | 结果 |
|------|-----------|-----------|-------------|------|
| 普通笔记本 | 1.0 | 100% | 100% | ✅ 正常 |
| Retina Mac | 2.0 | 100% | 200% | ✅ 正常（已适配） |
| 4K 显示器 | 1.5 | 100% | 150% | ✅ 正常（已适配） |
| 普通显示器 | 1.0 | **125%** | 100% | ❌ 比例异常 |
| 高分屏 | 2.0 | 100% | **110%** | ❌ 比例异常 |
| 多显示器 | 1.0/2.0 | 100% | 不一致 | ⚠️ 可能异常 |

---

## 💡 预防措施

### 开发时

1. **始终使用 100% 浏览器缩放**
2. **定期检查诊断输出**
3. **在多种缩放比例下测试**

### 发布时

1. **添加用户提示**（可选）：
```typescript
// 在 App.tsx 中添加
useEffect(() => {
  const cssZoom = window.outerWidth / window.innerWidth;
  if (Math.abs(cssZoom - 1) > 0.01) {
    console.warn('⚠️ 检测到浏览器缩放，建议按 Ctrl+0 重置为 100%');
  }
}, []);
```

2. **在文档中说明最佳设置**
3. **提供一键重置功能**（可选）

---

## 🆘 仍然有问题？

### 收集诊断信息

1. 按 F12 打开控制台
2. 运行 `diagnoseDisplay()`
3. 截图保存输出
4. 记录以下信息：
   - Edge 版本（地址栏输入 `edge://version`）
   - Windows 版本（设置 → 系统 → 关于）
   - 显卡型号
   - 显示器分辨率和缩放设置

### 联系支持

提供以上信息，以便快速定位问题。

---

## ✅ 已完成的技术修复

### 1. index.html 优化
- ✅ 完善视口元标签
- ✅ 明确 html/body/root 尺寸
- ✅ 禁止用户缩放（防止意外操作）

### 2. NeuroScene.tsx 优化
- ✅ Canvas DPR 配置 `[1, 2]`
- ✅ 限制最大像素比为 2
- ✅ 自动适配高分屏

### 3. 诊断工具
- ✅ 自动运行诊断脚本
- ✅ 详细输出关键指标
- ✅ 智能问题检测和建议

---

最后更新：2026-06-05
